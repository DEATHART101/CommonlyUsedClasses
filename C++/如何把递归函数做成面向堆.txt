1.递归函数的所有的*不同*参数要存到stackobject中
2.递归函数中的每次递归的返回值要存到stackoject中
3.由*不同*参数直接产生的变量要合理存放
4.如果递归函数有返回值的话，做一个临时变量用来存储它
5.如果递归函数有返回值的话，要做一个返回值的地址来表示返回值要存到哪里去
6.如果要返回的话，只需要让excutingline = -1，并保证接下来运行的第一行代码是pop栈

*不同*是指那些不能根据传入的参数计算出来的，并且每次都会变化的参数，这么做是为了减小存储空间